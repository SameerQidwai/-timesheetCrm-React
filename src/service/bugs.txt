New bug Found
    if permission changes or edit... what to do with the person if he is arleady Loggedin 

    Dates disable everywhere ref => to opportuity milestone start and end date

    Need to create disable variable if any one of the multiselect already has certain action
        for now disable if only one is not matched

    skill update in contact person is not wokring like changing the skill on same field or it's level

    need to create only one or two resource component... generalize them for further use... 
            {PMResources, OTResources, PTResources, OMResources}
                make these four component into one or two only....

    global vairbale anything can be null
    global variable updating array
    global varaibale index
    
    cost-calcuator
        relation between variable and values

    post Gloal APi post action


shortcut 
    class component
        rce
    function component
    rfce

    imd→	import { destructuredModule } from 'module'
    ime→	import * as alias from 'module'
    ima→	import { originalName as aliasName} from 'module'

    imbrc→	import { Route, Switch, NavLink, Link } from react-router-dom'
    cdm→	componentDidMount = () => { }
    sst→	this.setState({ })
    est→	this.state = { }



    fof→	for(let itemName of objectName { }
    fin→	for(let itemName in objectName { }

updating 
    "antd": "^4.15.6",
xampp
    sudo service mysql stop
    sudo /opt/lampp/lampp start

// for table in BaseCalculator
const tableColumn = [
    {
        title: 'label',
        dataIndex: 'label',
        key: 'label',
        render: (text, record) => <div>{text}</div>,
        onCell: (record, index) => ({
            colSpan: record.colSpan,
        }),
    }
]
const tableBody = (contract, golobalVariables, holidays) => {
    let body = [
        {label: 'Annual Base Salary', value: contract?.remunerationAmount, colLableSpan: 2},
        {label: 'Daily Hours', value: formatFloat(contract?.dailyHours), colLableSpan: 2},
        {label: `Weekly Hours - (${contract?.dailyHours} * ${contract?.noOfDays} days)`, value: formatFloat(contract?.noOfHours), colLableSpan: 2},
        {label: `Billable Days per annum`, value: contract?.workDaysPerAnum, colLableSpan: 2},
        {label: `Hourly Base Rate - (${formatCurrency(contract?.remunerationAmount)}/${contract?.workDaysPerAnum}/${contract?.dailyHours})`, value: contract?.hourlyBaseRate, colLableSpan: 2},
    ]

    golobalVariables.forEach((el, index)=> {
        body.push( {label: STATES[el.name]? `Payroll Tax - ${STATES[el.name]}` : el.name, value: el.value, amount: el.amount} )
    })

    body.push(
        {label: 'Adjustment', value: <Inputnumber
            value={adjustment}
            shape="$"
            onChange={onChangeAdjustment}
        />, colLableSpan: 2},
        {label: 'Employee Hourly Buy Rate', value: formatCurrency(variableCount), colLableSpan: 2},
        {label: 'Daily Billable Hours', value: formatFloat(contract?.dailyHours)},
        {label: 'Employee Daily Buy Rate', value: formatFloat(contract?.dailyHours * variableCount), colLableSpan: 2},
    )
}

